plugins {
    id 'org.abelsromero.nativ.java-application-conventions'
    id 'org.graalvm.buildtools.native' version '0.9.11'
}

dependencies {
    implementation project(':utilities')
}

application {
    mainClass = 'org.abelsromero.nativ.App'
}
